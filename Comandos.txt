ALLTRIM()
Remove os espaços em branco do início e do fim de uma cadeia de caracteres.

cell()
Retorna objeto da classe TRCell (célula) baseado no parametro informado. Se não for encontrado o objeto segundo o parametro informado será retornado Nulo

dbGoTop()
Move o cursor da área de trabalho ativa para o número do registro (Recno) especificado, realizando um posicionamento direto, sem a necessidade de uma busca (Seek) prévia.

dbSeek()
Pesquisa a área de trabalho ativa para encontrar um determinado registro, de acordo com a chave de pesquisa utilizada.

dbSetOrder()
Indica o número que representa a ordem do índice que será utilizado -

dbSkip()
Move o cursor do registro posicionado para o próximo registro (ou anterior, dependendo do argumento), de acordo com o índice ativo para a área de trabalho

EOF()
Determina se o final do arquivo de dados foi atingido.

FCount()
Retorna a quantidade de campos existentes na estrutura da área de trabalho ativa.

FieldPut()
Recupera ou lê o valor de um campo, de acordo com a sua posição dentro da estrutura do arquivo de dados, fornecendo o seu conteúdo.

FIELPOS()
A função tem a funcionalidade de retornar a posição de um determinado campo dentro da estrutura do alias corrente. Caso o campo não exista na estrutura, é retornado zero.Assim, sempre que for se referenciar um determinado campo, criado por um compatibilizador, ou criado em versão/release, proteja a execução com a função FieldPos.


FT_FEOF()
Indica se o ponteiro está posicionado no fim do arquivo texto.

FT_FGOTOP()
Posiciona no início (primeiro caracter da primeira linha) do arquivo texto aberto pela função 

FT_FREADLN()
Lê e retorna uma linha de texto do arquivo aberto pela função FT_FUse()

FT_FReadLn()
Retorna a linha inteira na qual está posicionado o ponteiro para leitura de dados.

FT_FSKIP()
Move o ponteiro, do arquivo texto aberto pela função FT_FUse(), para uma nova posição

FT_FUSE()
Ler arquivo de texto (TXT)

IncProc
A função  é utilizada para incrementar valores na régua de progressão, criada através da função ProcRegua()

Join()
Seleciona todas as linhas a partir das duas tabelas onde existam co-incidências de dados em relação às colunas especificadas para o Join.

len() 
Retorna o tamanho de uma cadeia de caracteres ou a quantidade de elementos em um array.

Local aOrd  
"Matriz contendo as ordens de layout para a impressão. Caso não existam várias
ordens esta matriz deve estar vazia. Ex.: aOrd := {""Código"", ""Descrição"",
""Telefone""} -> O layout do relatório vai depender da ordem selecionada na tela
de configuração de impressão"

SetDefault
Esta função cria uma intefaca onde a opção de imprimir o relatorio podem ser configuradas; duas variaveis ;; Uma controla numero de paginas e a outra vetor ocndendo as opções d impressão 



